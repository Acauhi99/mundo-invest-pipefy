name: CI/CD

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
      - run: |
          cd modules/client && golangci-lint run ./... && cd ../..
          cd modules/webhook && golangci-lint run ./... && cd ../..
          cd cmd/server && golangci-lint run ./... && cd ../..

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - run: go test -buildvcs=false github.com/mundoinvest/client/... github.com/mundoinvest/webhook/... github.com/mundoinvest/pipefy/... github.com/mundoinvest/shared/...
      - run: cd cmd/server && go test -buildvcs=false -count=1 ./... || true

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - run: |
          cd cmd/server && govulncheck ./... && cd ../..
          cd modules/client && govulncheck ./... && cd ../..
          cd modules/webhook && govulncheck ./... && cd ../..
          cd pkg/shared && govulncheck ./... && cd ../..
          cd pkg/pipefy && govulncheck ./... && cd ../..

  docker:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/mundo-invest-pipefy:latest

  deploy-fake:
    needs: docker
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying to AWS..."
